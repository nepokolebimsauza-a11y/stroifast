# Скопируйте в .env и заполните (файл .env не коммитить).
BOT_TOKEN=
CHAT_ID=1859327629

# Порт локального сервера (туннель должен вести сюда).
PORT=8787

# Список Origin через запятую (страница с формой + локальная отладка).
# Пример для GitHub Pages и VS Code Live Server:
ALLOWED_ORIGINS=https://nepokolebimsauza-a11y.github.io,http://127.0.0.1:5500,http://localhost:5500

# Опционально: писать копию заявок в файл (JSONL).
# LEAD_LOG_FILE=telegram_leads.log
